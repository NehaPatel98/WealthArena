apiVersion: v1
kind: Service
metadata:
  name: wealtharena-rl-service
  labels:
    app: wealtharena-rl
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app: wealtharena-rl
